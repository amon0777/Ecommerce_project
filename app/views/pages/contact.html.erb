<!-- contact.html.erb -->
<h1><%= @page.title %></h1>

<% if @page.description.present? %>
  <div class="page-description">
    <%= @page.description %>
  </div>
<% end %>

<% if @page.body.present? %>
  <div class="page-body">
    <%= @page.body %>
  </div>
<% end %>
